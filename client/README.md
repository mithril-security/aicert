# Requirements

Ubuntu 
```
sudo apt-get update
sudo apt-get install -y tpm2-tools
```

# Real 

`aicert verify`
`aicert new`
`aicert build --url`

# Simulation

Set the following environment variable: 
`AICERT_SIMULATION_MODE=1`
